default_platform(:ios)

platform :ios do
  desc "SwiftLint 실행"
  lane :lint do
    swiftlint(
      mode: :lint,
      config_file: ".swiftlint.yml",
      strict: true
    )
  end

  desc "개발 빌드"
  lane :build do
    build_app(
      project: "Vellure.xcodeproj",
      scheme: "Vellure",
      configuration: "Debug",
      skip_codesigning: true,
      skip_archive: true
    )
  end

  desc "테스트 실행"
  lane :test do
    run_tests(
      project: "Vellure.xcodeproj",
      scheme: "Vellure",
      device: "iPhone 16",
      clean: true
    )
  end

  desc "TestFlight 배포"
  lane :beta do
    increment_build_number
    build_app(
      project: "Vellure.xcodeproj",
      scheme: "Vellure",
      configuration: "Release"
    )
    upload_to_testflight
  end

  desc "App Store 배포"
  lane :release do
    increment_build_number
    build_app(
      project: "Vellure.xcodeproj",
      scheme: "Vellure",
      configuration: "Release"
    )
    upload_to_app_store(
      skip_screenshots: true,
      skip_metadata: true
    )
  end
end
